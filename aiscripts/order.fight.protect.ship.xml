<?xml version='1.0' encoding='utf-8'?>
<diff>
<!-- add. purpose: params.
  <params>
-->
  <add sel="//params">
    <param name="kAAITParam_isAvoidHighRisk" required="false" type="bool" text="{1111920, 3}" default="
      if this.$kAAIT_isAvoidHighRisk_user? then 
        this.$kAAIT_isAvoidHighRisk_user
      else (
        if (not this.assignedcontrolled.isunit) and (not this.assignedcontrolled.islasertower) and
        (
          (
            this.assignedcontrolled.isplayerowned and
            this.assignedcontrolled.isclass.[class.ship_xs, class.ship_s, class.ship_m] and
            @global.$kAAIT_Config.$xssm_isAvoidHighRisk
          ) or
          (
            (not this.assignedcontrolled.isplayerowned) and
            this.assignedcontrolled.isclass.[class.ship_xs, class.ship_s, class.ship_m] and
            @global.$kAAIT_Config.$xssm_isAvoidHighRisk_allFactions
          ) or
          (
            this.assignedcontrolled.isplayerowned and
            this.assignedcontrolled.isclass.[class.ship_l, class.ship_xl] and
            (
              @global.$kAAIT_Config.$lxl_isAvoidHighRisk_vShip or
              @global.$kAAIT_Config.$lxl_isAvoidHighRisk_vStation
            )
          ) or
          (
            (not this.assignedcontrolled.isplayerowned) and
            this.assignedcontrolled.isclass.[class.ship_l, class.ship_xl] and
            (
              @global.$kAAIT_Config.$lxl_isAvoidHighRisk_vShip_allFactions or
              @global.$kAAIT_Config.$lxl_isAvoidHighRisk_vStation_allFactions
            )
          )
        ) then true else false
      )
    "/>
  </add>
<!-- add before. purpose: show movement.
  <location object="$target" condition="$target.exists" />
-->
  <add pos="before" sel="//location[@object=&quot;$target&quot;][@condition=&quot;$target.exists&quot;]">
    <location object="@this.$kAAIT_data.$orderLocation_space" position="@this.$kAAIT_data.$orderLocation_pos" condition="@this.$kAAIT_data.$orderLocation_space and @this.$kAAIT_data.$orderLocation_pos" />
  </add>
<!-- add. purpose: withdraw ai.
  <interrupts>
-->
  <add sel="//interrupts">
    <handler ref="kAAIT_Handler_WithdrawOrJustFight" />
  </add>
<!-- add. purpose: debug.
  <init>
-->
  <add sel="//init">
    <debug_text text="this.assignedcontrolled.idcode + ' EVENT init'" chance="if @this.assignedcontrolled.idcode == @global.$kAAIT.$debugChance_idCode then @global.$kAAIT_Config.$debugChance else 0" />
  </add>
<!-- add. purpose: move or avoid.
  <move_to object="this.ship" destination="this.ship.zone" forceposition="false" forcerotation="true" commandaction="false">
-->
  <!-- <add pos="before" sel="//move_to[@object=&quot;this.ship&quot;][@destination=&quot;this.ship.zone&quot;]">
    <do_if value="@$kAAITParam_isAvoidHighRisk and (not @this.$kAAIT_data.$isIgnoreKAAIT)">
      <include_interrupt_actions ref="kAAIT_AvoidOrNot" />
    </do_if>
  </add> -->
<!-- add. purpose: move or avoid.
  <move_to object="this.ship" destination="$target.zone" uselocalhighways="false" forceposition="false" finishonapproach="true" commandaction="false" >
-->
  <add pos="before" sel="(//move_to[@object=&quot;this.ship&quot;][@destination=&quot;$target.zone&quot;])[1]">
    <do_if value="@$kAAITParam_isAvoidHighRisk and (not @this.$kAAIT_data.$isIgnoreKAAIT)">
      <set_value name="$kAAIT_avoidTest_space" exact="$target.zone" />
      <set_value name="$kAAIT_avoidTest_position" exact="$pos" />
      <include_interrupt_actions ref="kAAIT_AvoidOrNot" />
      <remove_value name="$kAAIT_avoidTest_space" />
      <remove_value name="$kAAIT_avoidTest_position" />
    </do_if>
  </add>
<!-- add. purpose: move or avoid.
  <move_to object="this.ship" destination="$target.zone.destination" uselocalhighways="not this.ship.isclass.[class.ship_xl, class.ship_l]">
-->
  <add pos="before" sel="//move_to[@object=&quot;this.ship&quot;][@destination=&quot;$target.zone.destination&quot;]">
    <do_if value="@$kAAITParam_isAvoidHighRisk and (not @this.$kAAIT_data.$isIgnoreKAAIT)">
      <set_value name="$kAAIT_avoidTest_space" exact="$target.zone.destination" />
      <set_value name="$kAAIT_avoidTest_position" exact="$safepos" />
      <include_interrupt_actions ref="kAAIT_AvoidOrNot" />
      <remove_value name="$kAAIT_avoidTest_space" />
      <remove_value name="$kAAIT_avoidTest_position" />
    </do_if>
  </add>
<!-- add. purpose: move or avoid.
  <move_to object="this.ship" destination="$target.zone" uselocalhighways="false">
-->
  <add pos="before" sel="(//move_to[@object=&quot;this.ship&quot;][@destination=&quot;$target.zone&quot;])[2]">
    <do_if value="@$kAAITParam_isAvoidHighRisk and (not @this.$kAAIT_data.$isIgnoreKAAIT)">
      <set_value name="$kAAIT_avoidTest_space" exact="$target.zone" />
      <set_value name="$kAAIT_avoidTest_position" exact="$target.position" />
      <include_interrupt_actions ref="kAAIT_AvoidOrNot" />
      <remove_value name="$kAAIT_avoidTest_space" />
      <remove_value name="$kAAIT_avoidTest_position" />
    </do_if>
  </add>
</diff>
